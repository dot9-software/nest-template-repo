/**
 * This is for static routes that won't be checked by the AuthGuard
 * i.e. if you want the route `/abc` not secured by the auth guard, add `"/abc"` to this list
 */
export const UNCHECKED_ROUTES = [];
